/**
 * AI boundary (Milestone 3).
 *
 * The only package that talks to model providers. Exposes narrow, versioned,
 * schema-validated operations (map concepts, generate lesson, grade response)
 * and owns no business state. Repository content is always untrusted input.
 */
export {};
